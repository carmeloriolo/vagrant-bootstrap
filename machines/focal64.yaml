---
configs:
  name: focal64
  box: ubuntu/focal64
  playbooks_prefix: './playbooks'
  scripts_prefix: './scripts'
  memory: 4096
  cpus: 2
  install_go: always
  install_docker: always
  install_kubeadm: never
  go_version: 1.17.9
  shared_mounts:
    - from: "~/Dev"
      to: "/home/vagrant/dev"
